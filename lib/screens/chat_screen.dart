import 'package:flutter/material.dart';

// This file is no longer used in the new Rasta app structure
// The chat functionality has been moved to FloatingTutor component
class ChatScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This screen is deprecated'),
      ),
    );
  }
}